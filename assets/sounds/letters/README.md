# Sons das letras

Os arquivos de áudio **não são versionados** (ficam apenas no seu computador).

Coloque aqui os arquivos `.m4a` com o nome de cada letra:

- `A.m4a`, `B.m4a`, `C.m4a`, … até `Z.m4a`

O app usa esses arquivos para reproduzir o som ao tocar em cada letra.
